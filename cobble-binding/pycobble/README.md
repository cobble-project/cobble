# pycobble

`pycobble` is the official high-performance Python binding for Cobble.

The package requires CPython 3.11 or newer. Its native extension is exposed as
`pycobble._native`; applications should import the public names from `pycobble`.
